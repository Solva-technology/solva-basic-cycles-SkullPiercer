"""
Реализуйте функции с использованием циклов.
Не меняйте названия функций.
"""

def sum_list_elements(lst: list) -> int:
    """
    Используя цикл for, посчитайте и верните сумму всех чисел в списке.
    
    Входящие данные:
    lst -- список чисел.
    
    Пример: [1, 2, 3, 4] -> 10
    """
    total = 0  # Здесь будем хранить сумму
    
    # TODO: Напишите цикл for, который перебирает элементы `lst`
    # и добавляет каждый элемент к `total`.
    
    # Этот код возвращает результат для проверки. Не меняйте его.
    return total

def find_max_element(lst: list) -> int:
    """
    Найдите и верните максимальный элемент в списке.
    Не используйте встроенную функцию max().
    
    Входящие данные:
    lst -- список чисел.
    
    Пример: [1, 9, 3, 5] -> 9
    """
    if not lst:
        return None  # Возвращаем None, если список пуст

    max_val = lst[0]  # Предположим, что первый элемент — максимальный
    
    # TODO: Напишите цикл for, который сравнивает каждый элемент
    # с `max_val` и обновляет `max_val`, если найден больший элемент.
    
    return max_val

def count_even_numbers(lst: list) -> int:
    """
    Посчитайте и верните количество четных чисел в списке.
    
    Входящие данные:
    lst -- список чисел.
    
    Пример: [1, 2, 3, 4, 5, 6] -> 3
    """
    count = 0  # Счетчик четных чисел
    
    # TODO: Напишите цикл for. Внутри цикла проверьте, является ли
    # число четным (остаток от деления на 2 равен 0).
    # Если да, увеличьте `count`.
    
    return count

def create_string_from_list(lst: list) -> str:
    """
    Создайте и верните одну строку из всех элементов списка, соединенных вместе.
    
    Входящие данные:
    lst -- список строк.
    
    Пример: ["hello", " ", "world"] -> "hello world"
    """
    result_string = ""
    
    # TODO: Напишите цикл for, который добавляет (конкатенирует)
    # каждый элемент списка к `result_string`.
    
    return result_string

def sum_until_negative(lst: list) -> int:
    """
    Используя цикл while, считайте сумму чисел в списке до тех пор,
    пока не встретится отрицательное число.
    
    Входящие данные:
    lst -- список чисел.
    
    Пример: [1, 2, 5, -1, 3, 4] -> 8
    """
    total = 0
    index = 0
    
    # TODO: Напишите цикл while, который работает, пока `index`
    # меньше длины списка и элемент `lst[index]` не отрицательный.
    # Внутри цикла добавляйте элемент к `total` и увеличивайте `index`.
    
    return total
